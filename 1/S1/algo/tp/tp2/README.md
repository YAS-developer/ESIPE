Bonjour je vais vous expliquer ce que j'ai effectue comme test.




test_verify_palindrome_rec1, ici on test le cas ou c'est un palindrome.

test_verify_palindrome_rec2, ici on test le cas ou ca ne l'est pas.


test_verify_palindrome1, ici on test le cas ou c'est un palindrome.

test_verify_palindrome2, ici on test le cas ou ca ne l'est pas.

Exercice 2

On peut les tester. Mais comme vous l'avez dit ca prend pas mal de temps a pour verifier la chaine de caracteres.







